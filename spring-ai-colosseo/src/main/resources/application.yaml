spring:
  ai:
    ollama:
      base-url: http://localhost:11434
      chat:
        options:
          model: qwen2.5:latest

translation:
  max-text-length: 10000
  translation-timeout: 60
  analysis-timeout: 90

server:
  port: 8081

logging:
  level:
    org.feuyeux.ai.springai.colosseo: DEBUG
    org.springframework.ai: DEBUG